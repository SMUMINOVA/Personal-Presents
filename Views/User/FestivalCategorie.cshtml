@model List<Festival>

@{
    Layout = "_UserLayout";
} 
@foreach (var item in Model)
{
    <a   class=" btn btn-lg btn-success btn-block" href="~/User/FestivalCategories/@item.Id">@item.Name</a>
    <br>
}