@model List<Interest>

@{
    Layout = "_UserLayout";
} 

<div class = "row justify-content-center">
    @foreach (var item in Model)
    {
        <div class = "col-md-3">
            <a asp-action="InterestCategorie" asp-controller="User" method="post" asp-route-Id="@item.Id">@item.Name</a>
        </div>
    }
</div>