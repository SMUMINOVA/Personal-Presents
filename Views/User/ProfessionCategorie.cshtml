@model List<Profession>

@{
    Layout = "_UserLayout";
} 

@foreach (var item in Model)
{
    <a   class=" btn btn-lg btn-success btn-block" href="~/User/ProfessionCategories/@item.Id">@item.Name</a>
    <br>
}