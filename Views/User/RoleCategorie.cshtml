@model List<RoleForUser>

@{
    Layout = "_UserLayout";
} 

@foreach (var item in Model)
{
    <a   class=" btn btn-lg btn-success btn-block" href="~/User/RoleCategories/@item.Id">@item.Name</a>
    <br>
}